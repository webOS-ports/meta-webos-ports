# bluebinder environment (bluebinder.service EnvironmentFile). Empty on purpose:
# luneos-device-config shadows this file at boot for adaptations whose
# controller needs a workaround - see generators/80-bluebinder-env.

# Environment for pulseaudio.service (EnvironmentFile). Empty on purpose:
# luneos-device-config shadows this file at boot for adaptations that need
# variables here - see generators/70-pulseaudio-env.
